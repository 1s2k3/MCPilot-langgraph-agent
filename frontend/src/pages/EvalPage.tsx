/** 评估中心（M7 交付：Dataset 管理 / 运行记录 / Dashboard）。 */
export default function EvalPage() {
  return (
    <div className="flex h-full items-center justify-center text-slate-400">
      评估中心 — M7 交付（Dataset / 运行记录 / Dashboard）
    </div>
  );
}
